# Зависимости для запуска тестов.
# datasets и torch в тестах подменяются заглушками (см. conftest.py),
# поэтому здесь их нет — тестам не нужны ни сеть, ни модель.
# Версия gradio должна совпадать с sdk_version в README.md.
gradio==6.26.0
razdel==0.5.0
numpy
pytest
